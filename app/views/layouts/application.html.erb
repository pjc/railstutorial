<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title)) %></title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= render 'partials/html5_shim' %>
</head>
<body>
  <%= render 'partials/header' %>
  <div class="container">
    
    <!-- Either name partial different than flash variable or use locals as we do here to make sure the partial can use the flash variable without conflict -->
    <%= render partial: "partials/flash", locals: { flash: flash} %>

    <%= yield %>
    <%= render 'partials/footer' %>
    <%= debug(params) if Rails.env.development? %>
  </div>
</body>
</html>
